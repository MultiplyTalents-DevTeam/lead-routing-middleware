{
  "installCommand": "npm install --include=dev",
  "buildCommand": "npm run build -w @mt/web",
  "outputDirectory": "packages/web/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/[...path]"
    }
  ]
}
